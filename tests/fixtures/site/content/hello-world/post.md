# Hello

Minimal post that exercises the render → link → compose pipeline.

A wiki-link to [[second-post]] forces the link phase to resolve a
cross-reference and the resulting HTML should contain a real anchor.
