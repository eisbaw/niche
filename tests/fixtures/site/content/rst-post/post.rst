RST Format Smoke
================

This post is rendered through docutils, not comrak. The wiki-link to
[[hello-world]] should still resolve at the link phase.

If compose's content-format dispatch ever regresses to ``*.md``-only,
this post stops appearing in the built site and the smoke test fails.
