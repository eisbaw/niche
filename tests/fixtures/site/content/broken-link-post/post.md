# Broken Wiki-Link

This post intentionally links to [[no-such-slug]], a slug that does
not exist in the link registry. The link phase should render this as
an anchor with `class="wikilink broken-link"`, not silently drop or
hard-fail.
